sudo: required
dist: trusty

language: cpp
compiler:
  - gcc
  - clang

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:

install:
    - sudo apt-get remove --auto-remove clang

script:
  - $CXX --version
  - ls /bin
  - ls /usr/bin


