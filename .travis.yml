sudo: required
dist: trusty

language: cpp
compiler:
  - gcc
  - clang

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - tree

install:
    

script:
  - $CXX --version
  - sudo tree -d -l /


